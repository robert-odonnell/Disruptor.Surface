- [Target SurrealDB schema shape](project_target_schema.md) — recurring schema patterns and their C# mapping (optional details ref tagged `[Reference, Inline]`, option<record> parents, COMPUTED reverse children, TYPE RELATION edges, TO-unions, array<object> via SurrealArray)
- [Aggregates: roots, membership, cross-aggregate IDs](project_aggregates.md) — [AggregateRoot] marks the unit of load/coordination; membership via [Children] reachability; CG011 on conflict; cross-aggregate edges expose IRecordId reads + typed Session.Relate<TKind> mutations.
- [Model-scoped runtime metadata on [CompositionRoot]](project_model_scoped_runtime.md) — Workspace.Schema / Workspace.ReferenceRegistry / Workspace.ApplySchemaAsync as static partial fragments. No process globals; multiple Surface-generated assemblies coexist.
- [SurrealSession concurrency model — substrate-owned](project_concurrency_model.md) — app-owned SurrealTransaction; no application-level lease; conflicts surface natively as SurrealConflictException at COMMIT. In-memory snapshot is post-load read cache only.
- [Generated aggregate loader + IEntity.Hydrate (typed CBOR)](project_loader.md) — Load{Root}Async on user's [CompositionRoot]; single nested SELECT through tx.QueryAsync with typed $_rootId binding; IEntity.Hydrate(SurrealValue, IHydrationSink) walks via HydrationValue helpers (no JSON, no reflection).
- [Cascade-on-delete — re-anchored preview.47](project_reference_state.md) — DeleteAsync runs three-phase pre-flight via PlanDelete (Cascade+Unset to fixpoint, then steady-state Reject blockers, throws CascadeRejectException before wire dispatch). Library predicts; substrate enforces via REFERENCE ON DELETE clauses. IEntity.EnumerateReferences + SetReferenceTo emitted by generator.
- [[Inline] carves owned-sidecar refs out of [Reference]](project_inline_attribute.md) — plain `[Reference]` is foreign pointer (id-only at load); `[Reference, Inline]` is owned sidecar (loader emits `field.*`).
- [SurrealFormatter — only Identifier() validation remains](project_surreal_formatter.md) — trimmed preview.42 to the regex-validated `Identifier()` extension. RecordId / StringLiteral / RenderSurrealLiteral all gone; user values flow as typed SurrealValue bindings, not formatted text.
- [Writes: pure-setter scalars + async SaveAsync/UnrelateAsync for edges](project_write_unit.md) — [Property]/[Parent]/[Reference] emit pure backing-field setters (preview.40); edge creation goes through Session.SaveAsync(new TVariant { Source, Target }, tx), edge deletion through Session.UnrelateAsync<TKind> (preview.51). User-side domain verbs are async one-liners.
- [The generator does not look at user methods](project_method_names_are_inert.md) — all model attrs are AttributeTargets.Property only; methods on [Table] classes are plain user code calling the Session DSL.
- [Session DSL is the library's contract; method shape is user concern](feedback_session_is_the_contract.md) — Session is THE surface; the library does not impose method shape, naming, verbs, or domain expression on the user.
- [One catcher, many throwers — fail-closed at the session boundary](feedback_one_catcher_many_throwers.md) — single try/catch in CommitAsync; any failure closes the session and rethrows; everything else throws freely. Boundary translators (JSON probes, transport client) are sanctioned exceptions.
- [Don't conflate substantive refactors with polish](feedback_substantive_vs_polish.md) — code bloat / architectural spread / implementation laziness / blindspots / real bugs are the substantive categories. Polish (renames, doc tweaks, formatting) comes later. Name the category accurately.
- [Sync read-side, async dispatch boundary](feedback_async_is_methods.md) — entity reads + Track + property setters are sync against the in-memory snapshot. Async surface is the dispatch boundary: SaveAsync / DeleteAsync / UnrelateAsync / FetchAsync / Load{Root}Async / Workspace.Query terminals.
- [Per-relation union interfaces (entity + id)](project_relation_unions.md) — generator emits I{InverseName} per multi-target kind (and I{InverseName}Id parallel) and I{Singularize(ForwardName)} per multi-source kind. Single-member sides skip emission.
- [Same-pass type resolution gotcha](project_same_pass_resolution.md) — user code can't reference generator-emitted types in `partial` member declared types. Method bodies (post-emit compile pass) can reference them fine.
- [Typed id design](project_typed_ids.md) — per-table {Name}Id readonly record structs wrapping a validated string; only two forms accepted (Ulid stringification or short lower_snake_case slug ≤32 chars), anything else throws.
- [Minimal library surface area](feedback_minimal_surface.md) — "use my library and I won't be intrusive"; prefer zero-line defaults and single-attribute escape hatches over inheritance/boilerplate.
- [/rpc JSON Thing-binding gotcha — RESOLVED 2026-05-12](project_surreal_rpc_binding.md) — historical: original justification for inlined record-id literals. CBOR-over-WS path (preview.29 SDK) + typed-CBOR end-to-end (preview.42) make this irrelevant. Treat as context, not action item.
- [Substrate-shadowing framing — RESOLVED 2026-05-12](project_substrate_shadowing.md) — historical: WriterLease/PendingState/CommitPlanner/relation-buffer all gone (preview.29/.34/.35/.45); INSERT RELATION IGNORE on deterministic-id moves idempotence to substrate (preview.49 reversed preview.46's UPSERT). Cascade re-anchored preview.47.
- [Step 0–5 unpainting plan — DONE 2026-05-12](project_unpainting_plan.md) — historical: Steps 0–3 shipped, Step 4 PARKED by design, Step 5 (cascade re-anchor) is feature-level not arch debt. preview.40/.45/.46 closed the "remaining polish" list.
- [SurrealDB v3 BEGIN returns a txn id in payloads](project_v3_txn_id.md) — cross-request txns travel by id, not socket. Connection pinning is a non-issue; HTTP can host multi-request txns. Verify before depending on.
- [SurrealDb.Net is "second-cousin energy"](project_dotnet_sdk_risk.md) — official .NET SDK lags Rust/Go/TS/Node, has CBOR + Thing-binding gaps. Building our own 1:1 client (Step 0) de-risks the unpainting plan.
- [Save to memory what I judge appropriate; don't make the user micromanage](feedback_memory_judgment.md) — after substantive conversations, capture conclusions without asking; tell user what was saved, not as permission request.
- [Disruptor.Surreal — first-pass v1 client at ../surrealdb-dotnet](project_disruptor_surreal_client.md) — Step 0 has shipped a CBOR-over-WS SDK; txn-id propagation + conflict exception in place; two concerns flagged (IConnection internal, embedded out).
- [Don't preserve existing behavior without the context](feedback_preserve_features_not_habits.md) — distinguish designed properties from habits. When a "constraint" forces all options to feel like compromises, often the wrong thing is being preserved.
- [Relation-as-class redesign — preview.51 DONE 2026-05-12](project_relation_as_class_redesign.md) — ForwardRelation<TPayload> replaced by [Restricts]-on-class + [In]/[Out]/[Property] variant entities. Edge mutations via `Session.SaveAsync(new TVariant { Source, Target }, tx)`. Multi-variant kinds = SCHEMALESS; single-variant = SCHEMAFULL.
- [Union endpoints — preview.54 DONE 2026-05-12](project_union_endpoints_design.md) — variants can carry [In]/[Out] typed to a user-declared `[Foo] partial interface IFooTarget : IRecordId`; members opt in via `partial interface IARecordId : IFooTarget`. All 5 phases shipped end-to-end: variant emitter Cartesian-expands hydration dispatcher, schema FROM/TO unions members, CG031 (kind mismatch) + CG032 (dead union) diagnostics, [Pertains] sample.
- [Release tag format — v0.1.0-preview.NN](project_release_tag_format.md) — release pipeline requires the leading `v` and full semver-style version (e.g. `v0.1.0-preview.54`); bare `preview.NN` tags don't trigger a build.
- [Shared-shape relation interfaces — preview.55 DONE 2026-05-12](project_shared_shape_relation_interfaces.md) — user-declared `partial interface : IRelationVariant` over variants with matching shape; generator emits `static I Create<TKind>(Action<I> init)` factory. Kind-keyed construction only; polymorphic queries stay user-side by design.
- [Annotated shared-shape lift — preview.56/.57 DONE 2026-05-13](project_annotated_shared_shape_lift.md) — interface members carry `[In]/[Out]/[Property]/[Id]`; variants (incl. empty-body `[Calls] partial class CallsRelation : IFoo;`) compose their shape via linker merge over the variant's annotated-interface closure. Local self-declared members and compatible interface fragments merge cleanly; hard conflicts flow to CG036.
- [CG036 lift-conflict diagnostic — DONE 2026-06-17](project_pending_cg036_lift_conflict_diagnostic.md) — shared-shape merge conflicts now report CG036 and name the variant/interface/member shapes instead of surfacing as a missing-emit symptom.
- [Design principle: user-declared interfaces as first-class model participants](project_user_interfaces_as_first_class_model_participants.md) — preview.54 + preview.55 + preview.56 all fell out of the same shape (user declares partial interface deriving from a runtime marker; members enrol via base list; generator emits a partial fragment onto the user's type). Try this shape before adding new attributes or model records.
